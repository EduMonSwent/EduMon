package com.android.sample.ui.theme

/**
 * IDs that StarterSelectionScreen emits and that we persist in the user profile. Keep them in one
 * place so theme / sprites / onboarding stay in sync.
 */
object EduMonStarterIds {
  const val FIRST = "pyromon" // uses default / current violet-blue theme
  const val SECOND = "aquamon" // warm red–orange–yellow theme
  const val THIRD = "floramon" // green theme
}
